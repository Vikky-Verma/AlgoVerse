import ComingSoon from "./ComingSoon";
import { Sparkles } from "lucide-react";

const Pricing = () => (
  <ComingSoon
    icon={Sparkles}
    title="Pricing"
    phase=""
    description="Plans and pricing are being finalized. AlgoVerse is free to use during early access."
    features={[]}
  />
);

export default Pricing;
